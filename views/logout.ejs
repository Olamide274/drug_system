<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="styles/logout.css">
    <link rel="stylesheet" href="styles/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="styles/fontawesome/css/all.css">
</head>
<body>
    <div class="logout-container">
        <div class="brand">DrugVerify</div>
        <div class="logout-card">
            <i class="fas fa-sign-out-alt logout-icon"></i>
            <h2 class="logout-title">Logout Confirmation</h2>
            <p class="logout-message">Are you sure you want to end your session?</p>
            
            <div class="btn-group">
                <button class="btn-cancel" onclick="window.history.back()">
                    <i class="fas fa-times"></i> Cancel
                </button>
                <button class="btn-logout" onclick="window.location.href='/logout/confirm'">
                    <i class="fas fa-sign-out-alt"></i> Logout
                </button>
            </div>

            <div class="session-info">
                <p>Logged in as: <strong>Admin</strong></p>
               
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // You can add any additional logout logic here
        document.querySelector('.btn-logout').addEventListener('click', function() {
            // Add any cleanup or session termination logic
            localStorage.clear();
            sessionStorage.clear();
            // Redirect to login page after logout
            window.location.href = '/login';
        });
    </script>
</body>
</html>